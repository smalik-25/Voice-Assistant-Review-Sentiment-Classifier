# Slim runtime for the containerized inference service (Phase 5).
# The Dockerfile installs only this file. It carries the sklearn control's
# runtime plus the API layer, nothing from the deep-learning stack.
scikit-learn==1.7.2
numpy==2.2.6
pandas==2.3.3
fastapi==0.139.0
uvicorn==0.49.0
pydantic==2.13.4
