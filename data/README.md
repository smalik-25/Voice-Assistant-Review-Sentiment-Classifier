# Data

Amazon Alexa reviews dataset (`amazon_alexa.tsv`).

- Source: Kaggle, "Amazon Alexa Reviews" (sid321axn/amazon-alexa-reviews).
- Place the file at `data/raw/amazon_alexa.tsv`. It is gitignored and never committed.
- ~3150 rows: `rating`, `date`, `variation`, `verified_reviews`, `feedback`.
